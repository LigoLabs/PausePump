# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
build/
.pub-cache/
.pub/
pubspec.lock

# android/ is committed (with its native config). iOS/web/desktop shells are
# generated on demand (flutter create) and kept out of git — see README.md.
/ios/
/web/
/macos/
/linux/
/windows/

# Machine-specific / generated bits inside android/
android/local.properties
android/.gradle/
android/.kotlin/
android/app/.cxx/
**/GeneratedPluginRegistrant.*

# IDE
.idea/
.vscode/
*.iml
